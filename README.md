# Naive-Bayes-Classifier-PCA
Featuring scratch implementations of the Naive Bayes classifier and Principal Component Analysis (PCA) using NumPy. The project includes comparative experiments across categorical and numerical datasets, covering baseline evaluation, feature selection, and dimensionality reduction analysis with supporting visualizations.
